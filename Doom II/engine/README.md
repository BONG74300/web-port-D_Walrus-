# Engine sources

The install script downloads DoomGeneric and DoomKit into this folder.

Run the installer and then build:

./scripts/install.sh
./scripts/build-engine.sh

Preserve upstream license notices.
